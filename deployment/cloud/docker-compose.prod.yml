version: "3.9"

services:

  api:

    build:
      context: ../..

    ports:
      - "8000:8000"

    environment:

      ENVIRONMENT: production

      API_KEY: geo-aware-mro-prod

  redis:

    image: redis:7

    ports:
      - "6379:6379"
