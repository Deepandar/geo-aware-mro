pipeline:
  paths:
    input: data/processed/sku_master.csv
    output: data/processed/sku_output_staging.csv

  execution:
    auto_generate: false

  mlflow:
    run_name: sku_pipeline_staging

  logging:
    level: INFO
    format: json
