[pytest]

pythonpath = .

testpaths =
    tests

addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    --cov=src
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=70

filterwarnings =
    ignore::DeprecationWarning
